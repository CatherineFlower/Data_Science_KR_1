# Domain DDoS Monitoring — Data Science KR 1 + KR 4

> Настольное кроссплатформенное приложение с GUI для **мониторинга доменов** и автоматического **подсчёта количества DDoS-атак по домену**.  
> Стек: **Python 3.12+, PyQt5, PostgreSQL 13+, psycopg2**, ИИ-детектор на **IsolationForest**.

---

## 📋 Содержание

- [Установка и запуск](#-установка-и-запуск)
- [Подзадачи КР-4](#-подзадачи-кр-4)
  - [Задача 1: Расширенные средства группировки данных + модуль представлений](#задача-1-расширенные-средства-группировки-данных--модуль-представлений)
  - [Задача 2: Полноценное управление представлениями (VIEW)](#задача-2-полноценное-управление-представлениями-view)
  - [Задача 3: Поддержка материализованных представлений (MATERIALIZED VIEW)](#задача-3-поддержка-материализованных-представлений-materialized-view)
  - [Задача 4: Модуль для работы с Common Table Expressions (CTE)](#задача-4-модуль-для-работы-с-common-table-expressions-cte)
- [База данных](#-база-данных)
- [Основные возможности (КР-1, КР-2)](#-основные-возможности-кр-1-кр-2)

---

## 🚀 Установка и запуск

### Требования

- Python 3.11+
- PostgreSQL 13+
- Установленные зависимости (см. ниже)

### Установка зависимостей

```bash
cd Data_Science_KR_1/src
pip install -r requirements.txt
```

### Настройка базы данных

1. **Создайте файл `.env`** в папке `src`:
```env
# Database connection settings
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=ваш_пароль

# Admin credentials
ADMIN_LOGIN=admin
ADMIN_PASSWORD=123
```

2. **Инициализируйте базу данных:**
```bash
cd src
python init_db.py
```

### Запуск приложения

```bash
cd src
python mainApp.py
```

**Или используйте готовый скрипт:**
- Дважды кликните на `run.bat` в папке `src`

### Вход в систему

- **Администратор:** логин `admin`, пароль `123` (из `.env`)
- **Обычный пользователь:** создайте через кнопку "Создать аккаунт"

---

## 📦 Подзадачи КР-4

### Задача 1: Расширенные средства группировки данных + модуль представлений

#### 1.1. Расширенная группировка (ROLLUP, CUBE, GROUPING SETS)

**Описание:** Добавлена поддержка расширенных операторов группировки в конструкторе SELECT-запросов.

**Как проверить в интерфейсе:**

1. Запустите приложение и войдите как администратор
2. Откройте **Админ-панель** → кнопка **"SELECT"**
3. В конструкторе SELECT:
   - Выберите таблицу (например, `app.tracked_domain`)
   - Выберите столбцы для группировки
   - В разделе **"GROUP BY"** найдите опции расширенной группировки:
     - **ROLLUP** — создаёт иерархические итоги
     - **CUBE** — создаёт все возможные комбинации группировок
     - **GROUPING SETS** — позволяет указать конкретные наборы столбцов для группировки
4. Выберите один из типов группировки
5. Нажмите **"Выполнить"**
6. **Ожидаемый результат:** 
   - В сгенерированном SQL видно `GROUP BY ROLLUP(...)`, `GROUP BY CUBE(...)` или `GROUP BY GROUPING SETS(...)`
   - Результаты содержат итоговые строки с NULL в соответствующих столбцах

**Пример SQL:**
```sql
SELECT domain, current_state, COUNT(*) 
FROM app.tracked_domain 
GROUP BY ROLLUP(domain, current_state);
```

---

#### 1.2. Модуль управления представлениями (базовая версия)

**Описание:** Создан модуль для просмотра списка представлений в схеме.

**Как проверить в интерфейсе:**

1. В **Админ-панели** нажмите кнопку **"Управление представлениями"** (Views Manager)
2. В открывшемся окне:
   - Выберите схему `app` из выпадающего списка
   - Нажмите **"Обновить список"**
3. **Ожидаемый результат:**
   - Отображаются вкладки: **"Обычные VIEW"** и **"MATERIALIZED VIEW"**
   - На вкладке **"Обычные VIEW"** видны существующие представления (например, `v_domain_current_state`, `v_ddos_events_last_hour`)
   - На вкладке **"MATERIALIZED VIEW"** видны материализованные представления (если есть)

---

### Задача 2: Полноценное управление представлениями (VIEW)

**Описание:** Реализовано полное управление обычными представлениями: создание, просмотр, редактирование, удаление.

#### 2.1. Создание обычного VIEW

**Как проверить в интерфейсе:**

1. В окне **"Управление представлениями"** нажмите кнопку **"Создать VIEW"** внизу окна
2. В открывшемся диалоге:
   - Введите имя представления (например, `test_view`)
   - В большое текстовое поле введите SQL запрос: 
     ```sql
     SELECT domain, current_state FROM app.tracked_domain
     ```
   - Нажмите **"Создать"**
3. **Ожидаемый результат:** 
   - Появится сообщение об успешном создании
   - Новое представление появится в списке обычных VIEW
   - При клике на него отобразится введённое SQL-определение

**Важно:** В таблице `app.tracked_domain` столбец называется `current_state`, а не `state`.

---

#### 2.2. Просмотр определения VIEW

**Как проверить в интерфейсе:**

1. В окне **"Управление представлениями"** выберите вкладку **"Обычные VIEW"**
2. В списке слева кликните на любое представление (например, `v_domain_current_state`)
3. Нажмите кнопку **"Просмотреть определение"**
4. **Ожидаемый результат:** 
   - В правой части окна (или в отдельном диалоге) отображается SQL-определение представления
   - Определение начинается с `SELECT` и содержит корректный SQL

---

#### 2.3. Просмотр данных VIEW

**Как проверить в интерфейсе:**

1. Выберите представление в списке
2. Нажмите кнопку **"Просмотреть данные"**
3. **Ожидаемый результат:**
   - Открывается таблица с данными из представления
   - Данные отображаются в виде таблицы с колонками и строками
   - Можно прокручивать и просматривать все записи

---

#### 2.4. Редактирование VIEW

**Как проверить в интерфейсе:**

1. Выберите представление в списке
2. Нажмите кнопку **"Редактировать"**
3. В открывшемся диалоге:
   - Измените SQL-определение
   - Нажмите **"Сохранить"**
4. **Ожидаемый результат:**
   - Сообщение об успешном обновлении
   - При просмотре определения отображается новое SQL

---

#### 2.5. Удаление VIEW

**Как проверить в интерфейсе:**

1. Выберите представление в списке
2. Нажмите кнопку **"Удалить"**
3. Подтвердите удаление в диалоге
4. **Ожидаемый результат:**
   - Представление исчезает из списка
   - Появляется сообщение об успешном удалении

---

### Задача 3: Поддержка материализованных представлений (MATERIALIZED VIEW)

**Описание:** Реализовано управление материализованными представлениями с возможностью обновления данных.

#### 3.1. Создание MATERIALIZED VIEW

**Как проверить в интерфейсе:**

1. В окне **"Управление представлениями"** нажмите кнопку **"Создать MATERIALIZED VIEW"**
2. В открывшемся диалоге:
   - Введите имя (например, `test_mat_view`)
   - Введите SQL запрос:
     ```sql
     SELECT domain, COUNT(*) as cnt 
     FROM app.tracked_domain 
     GROUP BY domain
     ```
   - Отметьте чекбокс **"Загрузить данные сразу"** (WITH DATA)
   - Нажмите **"Создать"**
3. **Ожидаемый результат:**
   - Сообщение об успешном создании
   - Представление появится на вкладке **"MATERIALIZED VIEW"**
   - При выборе отображается размер представления

---

#### 3.2. Просмотр информации о MATERIALIZED VIEW

**Как проверить в интерфейсе:**

1. Перейдите на вкладку **"MATERIALIZED VIEW"**
2. Выберите любое материализованное представление в списке
3. **Ожидаемый результат:** 
   - В разделе **"Информация"** отображается:
     - Имя представления
     - Общий размер (например, "24 kB")
     - Размер данных (например, "8192 bytes")
     - Количество индексов
     - Комментарий (если задан)

---

#### 3.3. Обновление MATERIALIZED VIEW (REFRESH)

**Как проверить в интерфейсе:**

1. Выберите материализованное представление в списке
2. Нажмите кнопку **"Обновить (REFRESH)"**
3. **Ожидаемый результат:**
   - Появится сообщение об успешном обновлении
   - Данные в представлении обновятся (если были изменения в исходных таблицах)

---

#### 3.4. Обновление MATERIALIZED VIEW (CONCURRENTLY)

**Как проверить в интерфейсе:**

1. Выберите материализованное представление, у которого есть уникальный индекс
2. Нажмите кнопку **"Обновить (CONCURRENTLY)"**
3. **Ожидаемый результат:**
   - Обновление проходит без блокировки таблицы
   - Сообщение об успешном обновлении

**Примечание:** Для CONCURRENTLY обновления требуется уникальный индекс на представлении. Если индекса нет, появится сообщение об ошибке.

---

#### 3.5. Просмотр данных MATERIALIZED VIEW

**Как проверить в интерфейсе:**

1. Выберите материализованное представление
2. Нажмите кнопку **"Просмотреть данные"**
3. **Ожидаемый результат:**
   - Отображается таблица с данными из представления
   - Данные соответствуют состоянию на момент последнего обновления

---

### Задача 4: Модуль для работы с Common Table Expressions (CTE)

**Описание:** Реализован конструктор для создания запросов с использованием CTE (WITH-запросов).

#### 4.1. Создание CTE запроса

**Как проверить в интерфейсе:**

1. В **Админ-панели** нажмите кнопку **"CTE конструктор"**
2. В открывшемся окне:
   - Нажмите **"Добавить CTE"**
   - Введите имя CTE (например, `users_summary`)
   - Нажмите **"Использовать SelectBuilder"** для создания определения CTE
   - В SelectBuilder выберите таблицу и столбцы, создайте запрос
   - Вернитесь в CTE конструктор
3. Добавьте ещё один CTE (опционально):
   - Нажмите **"Добавить CTE"** снова
   - Создайте второе CTE (например, `active_domains`)
4. Создайте главный запрос:
   - Нажмите **"Использовать SelectBuilder"** для главного запроса
   - В главном запросе можно ссылаться на созданные CTE
5. Нажмите **"Сгенерировать SQL"**
6. **Ожидаемый результат:**
   - Отображается сгенерированный SQL в формате:
     ```sql
     WITH users_summary AS (
       SELECT ...
     ),
     active_domains AS (
       SELECT ...
     )
     SELECT ... FROM users_summary JOIN active_domains ...
     ```

---

#### 4.2. Выполнение CTE запроса

**Как проверить в интерфейсе:**

1. После генерации SQL нажмите **"Выполнить"**
2. **Ожидаемый результат:**
   - Отображается таблица с результатами запроса
   - Данные корректно обработаны через CTE

---

#### 4.3. Редактирование CTE

**Как проверить в интерфейсе:**

1. В списке CTE выберите нужное CTE
2. Нажмите **"Редактировать"**
3. Измените имя или определение
4. Нажмите **"Сохранить"**
5. **Ожидаемый результат:**
   - Изменения сохраняются
   - При генерации SQL используются обновлённые данные

---

#### 4.4. Удаление CTE

**Как проверить в интерфейсе:**

1. В списке CTE выберите CTE
2. Нажмите **"Удалить"**
3. **Ожидаемый результат:**
   - CTE удаляется из списка
   - При генерации SQL это CTE не включается

---

#### 4.5. Изменение порядка CTE

**Как проверить в интерфейсе:**

1. В списке CTE используйте кнопки **"Вверх"** / **"Вниз"** для изменения порядка
2. **Ожидаемый результат:**
   - Порядок CTE в списке меняется
   - При генерации SQL CTE располагаются в указанном порядке

---

#### 4.6. Сохранение CTE запроса как VIEW

**Как проверить в интерфейсе:**

1. Создайте и сгенерируйте CTE запрос
2. Нажмите кнопку **"Сохранить как VIEW"** (если доступна)
3. В открывшемся диалоге введите имя представления
4. Нажмите **"Создать"**
5. **Ожидаемый результат:**
   - Создаётся VIEW с SQL из CTE запроса
   - VIEW появляется в списке представлений

---

## 🗄️ База данных

### Схема `app`

**Основные таблицы:**
- `app_user` — пользователи системы
- `tracked_domain` — домены под мониторинг
- `metric_sample` — метрики трафика (10-секундные срезы)
- `domain_state_log` — журнал смен состояний доменов
- `protected_column` — защищённые столбцы (не подлежат удалению)

**Представления:**
- `v_domain_current_state` — текущее состояние доменов
- `v_ddos_events_last_hour` — события DDoS за последний час

**Типы данных:**
- `app.domain_state` — ENUM: `'active'`, `'ddos'`, `'downtime'`

### Инициализация БД

Для создания схемы и всех необходимых объектов выполните:
```bash
cd src
python init_db.py
```

---

## ✨ Основные возможности (КР-1, КР-2)

### Пользовательские функции

- Регистрация/вход в систему
- Управление доменами (добавление/удаление)
- Просмотр метрик и статистики
- Топ сбоев по доменам

### Администраторские функции

- **ALTER TABLE** — конструктор для изменения структуры таблиц
- **SELECT** — расширенный конструктор запросов с подзапросами, агрегатами, GROUP BY, HAVING
- **Search** — поиск по строкам (LIKE/ILIKE, POSIX-регэкспы, SIMILAR TO)
- **Строковые функции** — UPPER/LOWER, SUBSTRING, TRIM, LPAD/RPAD, CONCAT, LEFT/RIGHT
- **Мастер соединений** — конструктор JOIN (INNER/LEFT/RIGHT/FULL)
- **Управление представлениями** — создание и управление VIEW и MATERIALIZED VIEW
- **CTE конструктор** — создание запросов с Common Table Expressions

---

## 📝 Примечания

- Все DDL/DML операции выполняются транзакционно с предпросмотром SQL
- Все операции логируются в файл `app.log`
- При ошибках отображаются понятные сообщения на русском языке
- Приложение кроссплатформенное (Windows/macOS/Linux)

---

## 🔧 Технические детали

**Основные модули:**
- `db.py` — функции работы с БД
- `AdminWindow.py` — админ-панель
- `select_builder_dialog.py` — конструктор SELECT-запросов
- `views_manager_dialog.py` — управление представлениями
- `cte_builder_dialog.py` — конструктор CTE
- `alter_table_dialog.py` — конструктор ALTER TABLE
- `join_wizard_dialog.py` — мастер соединений
- `string_funcs_dialog.py` — строковые функции

**Файлы конфигурации:**
- `.env` — переменные окружения (БД, админ)
- `ddl.sql` — SQL скрипт создания схемы
- `requirements.txt` — зависимости Python

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в файле `app.log`
2. Убедитесь, что PostgreSQL запущен и доступен
3. Проверьте настройки в файле `.env`
4. Убедитесь, что схема БД создана (`python init_db.py`)
